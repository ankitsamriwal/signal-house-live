# Signal House - live behaviour-pattern identifier

Paste leads with cross-channel interaction history; Jev reads intent, trust, velocity and the active behaviour pattern per record and recommends the next move. Code builds the cross-channel timeline and touch math. `POST /api/analyze` calls TypeSafe Jev (`jev-latest`) server-side with `TYPESAFE_API_KEY`.
